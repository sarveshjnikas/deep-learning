# Deep Learning

In this repo I will be implementing deep neural networks.

> **Note:** Some of these projects were built separately and moved to GitHub afterwards, so the commit history may be slightly messy.

1. [Feed-forward neural network for binary classification, from scratch in NumPy](1_neural_net_from_scratch/README.md)
2. Residual neural networks for image classification
